<% if @group.farts.last %>
  <% if !@group.farts.last.delter_id && @group.farts.last.smelter_id == @current_user.id %>
      <%= button_to "I REGRET THIS", regret_path %>
  <% elsif !@group.farts.last.delter_id %>
    <%= button_to "I DELT THE FART", guilty_path %>
    <% else %>
    <%= button_to "I SMELL A FART", farts_path %>
  <% end %>
  <% else %>
  <%= button_to "I SMELL A FART", farts_path %>
<% end %>
